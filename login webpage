<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MLCT - My Legal Case Tracker</title>
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;600&family=Cormorant+Garamond:wght@600&family=Roboto+Mono:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body,
        html {
            height: 100%;
            font-family: 'Raleway', sans-serif;
            color: #111;
            overflow: hidden;
        }
        /* Background */
        
        .video-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -2;
        }
        
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(100, 100, 100, 0.4);
            backdrop-filter: blur(10px);
            z-index: -1;
        }
        /* Loader */
        
        #loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #641226;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            z-index: 9999;
            transition: opacity 1s ease, visibility 1s ease;
        }
        
        #loader.hidden {
            opacity: 0;
            visibility: hidden;
        }
        
        #loader svg {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
            fill: none;
            stroke: #fff;
            stroke-width: 2;
            animation: spin 2s linear infinite;
        }
        
        #loader h1 {
            color: #fff;
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            letter-spacing: 2px;
        }
        
        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
        /* Card */
        
        .card {
            max-width: 950px;
            width: 90%;
            display: grid;
            grid-template-columns: 1fr 1fr;
            border-radius: 20px;
            overflow: hidden;
            background: rgba(255, 255, 255, 0.55);
            backdrop-filter: blur(20px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.25);
            position: relative;
            z-index: 10;
            animation: fadeIn 1.2s ease-in-out;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .left {
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background: rgba(255, 255, 255, 0.7);
        }
        /* Brand */
        
        .brand {
            display: flex;
            align-items: center;
            gap: 18px;
            margin-bottom: 1.2rem;
        }
        
        .brand svg {
            width: 90px;
            height: 90px;
            flex-shrink: 0;
            fill: none;
            stroke: #641226;
            stroke-width: 2;
        }
        
        .brand h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.2rem;
            font-weight: 600;
            letter-spacing: 2px;
            color: #111;
        }
        
        .left h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            margin-bottom: 1.2rem;
            color: #641226;
        }
        
        .left p {
            font-size: 1rem;
            line-height: 1.6;
            color: #222;
        }
        /* Right forms */
        
        .right {
            position: relative;
            overflow: hidden;
        }
        
        .form-wrapper {
            display: flex;
            width: 200%;
            transition: transform .6s ease;
        }
        
        .form-container {
            width: 50%;
            padding: 2.5rem;
        }
        
        .form-container h2 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            font-family: 'Cormorant Garamond', serif;
            color: #641226;
        }
        /* 🔥 Labels & placeholders font */
        
        label {
            font-family: 'Roboto Mono', monospace;
            font-size: .9rem;
            font-weight: 600;
            display: block;
            margin-bottom: .3rem;
            color: #333;
        }
        
        input {
            width: 100%;
            padding: .8rem;
            border: 1px solid #ccc;
            border-radius: 10px;
            outline: none;
            margin-bottom: 1.2rem;
            font-size: .95rem;
            font-family: 'Roboto Mono', monospace;
        }
        
        input:focus {
            border-color: #641226;
            box-shadow: 0 0 0 2px rgba(100, 18, 38, 0.2);
        }
        
        input::placeholder {
            font-family: 'Roboto Mono', monospace;
            font-size: .9rem;
            color: #666;
        }
        /* Buttons */
        
        .btn {
            background: #641226;
            color: #fff;
            padding: .9rem;
            border: none;
            width: 100%;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
            transition: all .3s;
        }
        
        .btn:hover {
            background: #801732;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
        }
        
        .toggle-link {
            margin-top: 1rem;
            font-size: .9rem;
        }
        
        .toggle-link a {
            color: #641226;
            cursor: pointer;
            text-decoration: none;
            font-weight: 600;
        }
        
        .slide-signup {
            transform: translateX(-50%);
        }
        
        .container {
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        
        @media(max-width: 768px) {
            .card {
                grid-template-columns: 1fr;
            }
            .left {
                text-align: center;
            }
            .brand {
                justify-content: center;
            }
        }
    </style>
</head>

<body>

    <!-- Loader -->
    <div id="loader">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
      <circle cx="32" cy="32" r="28"/>
    </svg>
        <h1>MLCT</h1>
    </div>

    <!-- Background Video -->
    <video autoplay muted loop playsinline class="video-bg">
    <source src="https://cdn.pixabay.com/video/2021/07/31/84177-583094826_large.mp4" type="video/mp4">
  </video>
    <div class="overlay"></div>

    <!-- Card -->
    <div class="container">
        <div class="card">
            <!-- Left -->
            <div class="left">
                <div class="brand">
                    <!-- Inline SVG logo -->
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
            <circle cx="32" cy="32" r="28"/>
            <path d="M20 44 Q32 20 44 44" stroke-linecap="round" fill="none"/>
            <circle cx="32" cy="28" r="6"/>
          </svg>
                    <h1>MLCT</h1>
                </div>
                <h2>My Legal Case Tracker</h2>
                <p>We help you manage and track your legal cases with ease. Stay organized, connect with your legal team, and keep all your case details in one place.</p>
            </div>

            <!-- Right -->
            <div class="right">
                <div id="formWrapper" class="form-wrapper">
                    <!-- Login -->
                    <div class="form-container">
                        <h2>Login</h2>
                        <form>
                            <label>Email</label>
                            <input type="email" placeholder="Enter your email">
                            <label>Password</label>
                            <input type="password" placeholder="Enter your password">
                            <button type="submit" class="btn">Login</button>
                        </form>
                        <p class="toggle-link">Don’t have an account? <a onclick="toggleForm('signup')">Sign up</a></p>
                    </div>
                    <!-- Signup -->
                    <div class="form-container">
                        <h2>Sign Up</h2>
                        <form>
                            <label>Full Name</label>
                            <input type="text" placeholder="Enter your full name">
                            <label>Email</label>
                            <input type="email" placeholder="Enter your email">
                            <label>Password</label>
                            <input type="password" placeholder="Create a password">
                            <button type="submit" class="btn">Sign Up</button>
                        </form>
                        <p class="toggle-link">Already have an account? <a onclick="toggleForm('login')">Login</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- JS -->
    <script>
        function toggleForm(form) {
            const wrapper = document.getElementById('formWrapper');
            if (form === 'signup') wrapper.classList.add('slide-signup');
            else wrapper.classList.remove('slide-signup');
        }

        // Loader script
        window.addEventListener("load", () => {
            const loader = document.getElementById("loader");
            setTimeout(() => {
                loader.classList.add("hidden");
            }, 1200); // wait a bit for smooth transition
        });
    </script>
</body>

</html>
